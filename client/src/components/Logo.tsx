import styled from 'styled-components';

import { textThemeColor } from '../themeHelpers';

const Logo = styled.h1`
  font-style: normal;
  font-weight: bold;
  font-size: 32px;
  margin: 0;
  color: ${textThemeColor};
`;

export default Logo;
